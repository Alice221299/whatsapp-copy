const singUpForm = document.getElementById("singUpForm");

singUpForm.addEventListener("submit", (e) => {
    e.preventDefault();
    //const params = { singUpForm: singUpForm, url: URL_users};
    //SubmitMovimiento(params);
})