import { patchUser } from "../services/patchUser.js";


export const editUser = (form, input) => {
    form.addEventListener("submit", async () => {
        const userId = localStorage.getItem('userId');
        
    });
  }

